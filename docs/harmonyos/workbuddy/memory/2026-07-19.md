# 2026-07-19 工作日志

## Stellarium 移植纯血鸿蒙 — 可行性调研与路线讨论

- 用户目标：把 Stellarium 移植到纯血鸿蒙（HarmonyOS NEXT），并让 WorkBuddy 组建"专家团"推进。
- 场景补充：用户要求 PC（鸿蒙电脑）+ 平板 + 手机三端都能用。

### 关键发现（已 Web 查证）
- Qt 6.12.0 Beta2 已官方支持 HarmonyOS 构建目标（harmonyos_arm64_v8a），提供 harmonydeployqt 打包工具，需 DevEco Studio 6.1 + SDK API 23。
- Stellarium 本体是 Qt（C++/QML）；Stellarium Mobile 是 Qt Quick 版，社区有安卓/iOS 移植 fork。
- OpenHarmony NDK 支持 OpenGL ES 3.2（XComponent + EGL），原生路线可行但工作量大。
- vcpkg 有 OHOS 分支，triplet：arm64-ohos / arm-ohos / x64-ohos，可解第三方库交叉编译。

### 三条路线
- A. Qt 整包移植（推荐）：复用 Qt 源码交叉编译，多端覆盖性价比最高。风险=Qt for HarmonyOS 仍 Beta + 依赖交叉编译 + GLES 兼容。
- B. ArkUI 原生重写：最纯血、可接鸿蒙特性，但工作量大（月级）。
- C. ArkWeb 套壳 Web 版：最快上线，但非原生、体验弱。

### 当前状态
- 已用 AskUserQuestion 给出三选项；用户追问优缺点及三端推荐，已回复并推荐路线 A（以 Stellarium Mobile Qt 源码为基底，arm64 包覆盖三端）。
- 待用户确认路线后，用 expert-manager 流程建「Stellarium 鸿蒙移植专家团」（Team 型，初定 5 名成员）。

### 约束（重要）
- 本沙箱无法做真机编译/签名部署，需用户侧 DevEco Studio + 开发者证书 + 鸿蒙设备/模拟器。专家团交付物=移植方案 + 工具链搭建手册 + 可编译工程脚手架。

### 已完成（专家团已建并注册）
- 用户表态"不懂技术、全权委托 AI 办"，据此拍板走路线 A（Qt 整包移植）。
- 已用 expert-manager 流程建「Stellarium 鸿蒙移植专家团」（Team 型），校验通过并注册到 marketplace.json，在 WorkBuddy 专家中心可见。
- 专家包位置：~/.workbuddy/plugins/marketplaces/my-experts/plugins/stellarium-harmonyos-port
- 成员（6）：主理人 乔渡星（跨端移植总工）+ 匡编译(Qt跨端编译师) + 苏星图(天文架构师) + 包立成(鸿蒙构建发布师) + 赖库通(依赖移植工程师) + 严三端(多端质检师)。
- 已产出《Stellarium 鸿蒙移植方案与工具链手册》到工作区，含路线A步骤、环境清单、DevEco签名打包傻瓜步骤、风险回退。
- 待用户本地装好 DevEco+Qt for HarmonyOS 后，可让专家团进入 Phase2 真正编译；或先跑 Phase1 出《架构适配报告》。
- 注：avatars 未生成（避免占用图像额度），可后续用 ImageGen 补；不影响功能。

## Phase 1 完成：架构适配报告

- 用户确认 DevEco 已装好，要求出《架构适配报告》（不要头像，省图像额度）。
- 已产出：/Users/jiexuanyang/WorkBuddy/2026-07-19-01-20-21/Stellarium-鸿蒙架构适配报告.md
- **关键修正（重要）**：此前建议"以 Stellarium Mobile(Qt Quick)分支为基底"有误——Stellarium Mobile 是 Noctua 闭源商业软件，开源可白嫖的只有**桌面 GPL 版**。故路线 A 基底修正为 Stellarium 桌面开源版（Qt6），额外加触屏适配层 + GLES 渲染验证。
- 核查事实更新：
  - Qt 6.12.0 Beta2 目标 harmonyos_arm64_v8a；需 DevEco 6.1.0 + API 23；harmonydeployqt 打包；ohos-additional-packages 官方预编译第三方库。
  - Qt 鸿蒙不支持模块：WebEngine / Positioning / SerialPort / VirtualKeyboard / TextToSpeech / 旧 Qt OpenGL；Qt Sensors 支持（底层 libohsensor）。
  - 鸿蒙 NDK 传感器 API：libohsensor.so + oh_sensor.h，权限 ACCELEROMETER/GYROSCOPE。
  - 仅 arm64-v8a（华为全系满足）；Qt for HarmonyOS 仍 Beta。
- 风险分级：高=Qt Beta坑 + 桌面GLSL在GLES不兼容；中=冲突模块改写/触屏适配/星表体积；低=多架构不支持(无碍)。
- 下一步（Phase 2）：步骤0环境确认→步骤1最小渲染PoC(验证GLES+打包链路)→全量编译裁剪→触屏/传感器适配→资源打包三端验证→签名上架(用户做)。

## Phase 2 启动：工程脚手架并行产出

- 用户批准进入 Phase 2 并说"让专家团干"。本沙箱无 Qt 鸿蒙 SDK/DevEco，无法真编译，故专家团产出"可在用户本地编译运行的工程骨架+脚本"。
- 三位成员并行：包立成出环境检测脚本(check_env.sh+README)；匡编译+苏星图出最小渲染 PoC 工程(stellarium-poc/, 验证 GLES+harmonydeployqt+触屏)；赖库通+严三端出依赖交叉编译清单与三端验收 checklist。
- 产出 12 个文件，已 verify 质量（CMakeLists 工具链写法、check_env.sh 跨平台探测、module.json5 三端+权限均合理）。
- 汇总文档：phase2/PHASE2-进度汇总.md。
- 用户下一步：本地跑 check_env.sh 确认环境 → 按 stellarium-poc/README 编译最小 PoC 验证链路 → 通过后进入全量 Stellarium 编译裁剪。
- 沙箱限制已多次向用户说明：真编译/签名/装真机必须由用户在本地 DevEco 完成。

## Phase 2 深化：全量 Stellarium 移植工具包（基于真实源码核查）

- 用户说"DEV ECO 装好了、平板也连上了，你们自己解决"。已再次说明沙箱无法替其点 DevEco/摸平板，但可把全程自动化成可在本地跑的脚本+补丁。
- **关键查证（决定路线 A 是否真可行）**：
  - 稀疏克隆 Stellarium 官方仓库，实读 CMakeLists.txt 与 cmake/：确认它是 **Qt Widgets** 程序（非 QML）。
  - Web 查证 Qt Wiki：官方 example 用 `widgets/gallery` 成功编译到鸿蒙 → **Qt for HarmonyOS 支持 Widgets** → 路线 A 立住，无需重写 UI。
  - Qt HarmonyOS 模块清单：支持 Core/Gui(含OpenGL)/Widgets/Concurrent/Network/Sql/Svg/Qml/Quick；**不支持 WebEngine、VirtualKeyboard**；Charts/Multimedia/Sensors/Positioning/SerialPort/Speech 通常不在安装包（需自编译或关）。
  - Stellarium CMake 真实选项：ENABLE_QTWEBENGINE/SPEECH/GPS/INDI/MEDIA/SHOWMYSKY/XLSX/NLS/SCRIPTING；插件 TELESCOPECONTROL；line 632 无条件 REQUIRE Positioning（关 GPS 后冗余，需补丁移除）。
- **产出 9 文件工具包**：phase2/stellarium-ohos-port/
  - build/ohos-build-stellarium.sh（主脚本：探测add-on→缺Charts自动调build-qt-addons编→git apply补丁→按探测动态拼-D开关→qt-cmake编译→harmonydeployqt→patch module.json5）
  - build/build-qt-addons-ohos.sh（交叉编译 qtcharts 装进 OHOS Qt 前缀）
  - build/stellarium-ohos.cmake（开关缓存兜底）
  - patches/0001-ohos-cmake-adapt.patch（移除 632 行 Positioning REQUIRE；已 `git apply --check` 对真实源码验证 OK）
  - patches/FEATURES_DISABLED.md（8 项关闭功能说明）
  - deploy/patch-module-json5.py（自动加三端 deviceTypes + 传感器权限，已 py_compile 通过）
  - deploy/DEPLOY.md / QA-CHECKLIST.md / TROUBLESHOOT.md
  - README.md（总说明：结论/事实/策略/3 步跑法/风险）
- 策略拍板：关 WebEngine/VirtualKeyboard/GPS/INDI/Speech/Media/ShowMySky/XLSX/NLS；Charts 自动交叉编译；核心渲染/星表/星座/深空/AstroCalc/脚本/触屏全保留（主链路零损失）。
- 语法校验：bash -n 通过；python py_compile 通过；补丁 apply --check OK。
- 交付闭环：用户本地跑主脚本+DevEco签名装机；报错贴回→AI改代码迭代（TROUBLESHOOT.md 已写常见报错速查）。

## 重大进展：本机实测（用户追问"你们不能在这 Mac 上直接搞吗"）

- **确认本机=用户 Mac，AI 可直接在本机跑命令（非远程沙箱）**。此前"需在用户本地 DevEco 完成"的说法过度保守——本机已具备：
  - DevEco Studio 6.1.1.290 ✅（在 /Applications/DevEco-Studio.app）
  - hdc 见平板设备 `7LZBB26323200303`（已授权连接）→ 可命令行 `hdc install` 直接部署到平板，无需开 DevEco 点按钮
  - hvigorw 存在 ✅（DevEco 内）；git 2.54 ✅
  - 网络通 download.qt.io ✅；磁盘剩 ~690G ✅
  - **唯一缺口：Qt 6.12 HarmonyOS 工具链未装**（~/Qt、/opt/Qt、QtSDK 均无）；ohos-additional-packages 也未装
- **尝试自动装 Qt 撞墙**：下载 qt-online-installer-macOS-universal-4.11.0.dmg 并挂载。
  - `--list` 不是合法参数（报 Unknown option: list）
  - 即使带 --accept-licenses/obligations/confirm-command，仍进入 **graphical mode** 并卡在 **Qt Account 登录**（免费版也强制登录，"Could not find Qt Account"）
  - **结论：无界面安装被 Qt Account 登录卡死，需用户提供 Qt 账号 或 用户 GUI 登录一次**
- **待用户决策**：Qt 账号如何处理？
  - A) 用户 GUI 登录装一次（开安装器→登录→勾 HarmonyOS→安装），之后 AI 全包 build+deploy
  - B) 用户给 Qt 账号密码，AI 试静默装（AI 经手密码，仅本会话）
  - C) 用户无 Qt 账号→先去 qt.io 免费注册
- 安装器已下载在 /tmp/qt_installer/qt-installer.dmg，挂载卷 /Volumes/qt-online-installer-macOS-universal-4.11.0（仍挂载中，便于后续用）。

## 新障碍：Xcode 缺失 + aqtinstall 无 harmonyos

- 用户在 Qt 安装器遇到 "The required 'Xcode' installation was not found!"。实测：本机仅有 **Xcode Command Line Tools**（clang 21 可用、SDK 在），但**无完整 Xcode.app**；`xcodebuild` 需要 Xcode.app（CLT 不含）→ 安装器 Xcode 预检失败。
- `sudo` 在本环境被禁（"Operation not permitted"），故 xcodebuild 授权需用户在自行的 Terminal 或开 Xcode GUI 同意，不能由我 sudo。
- 试 aqtinstall（第三方命令行 Qt 安装器，免 Xcode/免登录）绕过：装在 managed venv，`aqt list-qt mac` 仅返回 `android desktop ios`，**无 harmonyos 目标** → 此路不通。
- **结论**：Qt 鸿蒙工具链只能经官方在线安装器获取，而它强制要完整 Xcode。可靠路径=用户从 App Store 装 Xcode（免费，~30GB，一次性）→ 开 Xcode 同意协议 → 再跑 Qt 安装器（登录+勾 HarmonyOS）。之后 AI 全包 build+deploy。
- 待用户决策：是否接受装 Xcode；或让 AI 继续找更轻的取巧路线（不确定能成）。

## 用户已装 Xcode + Qt 鸿蒙版，本机实跑全量构建（踩坑修复序列）

- 用户按引导：App Store 装 Xcode 并同意协议；Qt 在线安装器登录后勾 **Qt 6.12.0-beta2 → Desktop(macOS) + HarmonyOS(arm64-v8a) + Qt Charts**（实际装在 `/Users/jiexuanyang/Qt/6.12.0/`）。鸿蒙版 Qt Charts 自带（免交叉编译）。
- 我本机实测操作（非沙箱）：下载解压 ohos-additional-packages 到 `~/.local/opt/ohos/additional-packages`（sha1 校验通过，内含 zlib/libpng/freetype/fontconfig/icu/openssl 的 arm64-ohos .so）；完整克隆 Stellarium 源码到 `~/stellarium-src`；修正主脚本 Qt 路径（`~/Qt` 非 `~/QtSDK`）、补 DevEco node/jdk/cmake/ninja 到 PATH。
- **坑1（cmake 找不到）**：本机 PATH 无 cmake → 改用 DevEco SDK 自带 cmake（`.../native/build-tools/cmake/bin`）。已加进脚本 PATH。
- **坑2（chainload 失败→回退 AppleClang+找不到 Qt6）**：Qt 鸿蒙工具链 `qt.toolchain.cmake` 写死 chainload 路径 `/opt/harmonyos/command-line-tools/sdk/.../ohos.toolchain.cmake`（本机无）。修复=主脚本加 `-DQT_CHAINLOAD_TOOLCHAIN_FILE=${DEVECO}/Contents/sdk/default/openharmony/native/build/cmake/ohos.toolchain.cmake`，让 Qt 链加载 DevEco 真实 OHOS 交叉编译工具链。修复后编译器变 OHOS clang、Platform=OHOS-1(aarch64)、Qt6 找到、GLESv3 找到。
- **坑3（Qt6::Positioning 找不到）**：src/CMakeLists.txt:514 把 `Qt6::Positioning` **无条件**塞进链接列表，但 find_package(Positioning) 仅在 ENABLE_GPS=1 时（根 782 行）。关 GPS→未引入→链接失败。修复=补丁移除 514 行 Positioning（557 行在 ENABLE_GPS 内已按需加）。
- **坑4（Qt6::GuiPrivate 找不到）**：Stellarium 链接 GuiPrivate 的条件是 `Qt6>=6.10`（src:519-521 无条件加）；但 find_package(GuiPrivate)（根 643 行）条件是 `Qt6>=6.10 AND NOT USE_BUNDLED_QTCOMPRESS`，而 USE_BUNDLED_QTCOMPRESS 默认=1 → 查找被跳过 → target 未创建 → 链接失败。GuiPrivate 实际是 INTERFACE 接口库（仅加私有头文件路径，无 .so，OHOS Qt 已带 cmake 配置）。修复=补丁去掉 `AND NOT USE_BUNDLED_QTCOMPRESS` 闸门，使查找与链接条件一致。
- **补丁最终版**：phase2/stellarium-ohos-port/patches/0001-ohos-cmake-adapt.patch 含 3 个 hunk（根632 去Positioning REQUIRE / 根643 去GuiPrivate闸门 / src514 去无条件Positioning链接）；构建脚本改为 apply 前先 `git checkout` 两文件以保证可重复。
- **当前状态（02:27）**：构建第 5 次运行 `/tmp/stellarium_build5.log`，**配置+生成阶段已通过（Configuring done / Generating done）**，正进入 cmake --build 全量编译（最慢阶段，预计 20-40 分钟）。剩余警告均非致命：Qt6TaskTree（QmlAssetDownloaderPrivate 依赖，不影响）、exiv2 缺失（LensDistortionEstimator 插件，无关观星主功能）。后台任务 task ha1V51 运行中，完成会自动通知。
- 下一步：编译成功→harmonydeployqt 生成 DevEco 工程→patch-module-json5.py 修三端/权限→`hdc install` 推到平板（设备 7LZBB26323200303）。若编译期报错（如 GLSL/GLES 着色器、某私有 API 缺失），捞日志定位修补丁再跑。

### 可复用经验（Qt6→HarmonyOS 交叉编译移植）
- Qt for HarmonyOS 工具链必须显式 `-DQT_CHAINLOAD_TOOLCHAIN_FILE=<DevEco OHOS toolchain>` 覆盖写死的 /opt/harmonyos 路径，否则回退宿主编译器且找不到 Qt6。
- Qt6.10+ 把 FooPrivate 拆出独立组件，使用方必须 `find_package(Qt6 COMPONENTS FooPrivate)`，且要注意上游"链接条件"与"find_package 条件"是否一致（Stellarium 此例不一致）。
- 本机无 cmake/ninja 时直接用 DevEco SDK 自带（`.../native/build-tools/cmake/bin`）。
- 平板已授权时 `hdc install xxx.hap` 可命令行直装，免开 DevEco。

## 用户功能补全意愿（后续 Phase 3 输入）
- 用户明确表示想后续补回被关闭/待适配的功能，点选优先级顺序：①中文界面 ②维基百科图文 ③指哪看哪 ④GPS自动定位。
- AI 建议落地顺序按"先易后难"调整：中文界面(改开关最易)→指哪看哪(写 Qt Sensors 适配+权限)→GPS(接鸿蒙 LocationKit)→维基百科(最难，改用 ArkWeb 原生组件重写，非等 Qt)。
- 主程序编译装平板跑通后，从中文界面开刀作为第一个补功能里程碑。

## 坑5：QtPositioning 头文件硬 include（编译期 68% 失败，已修）
- 现象：全量编译至 68% 报 `src/core/StelLocationMgr.hpp:28: fatal error: 'QtPositioning/QGeoPositionInfoSource' file not found`，core + 依赖插件（Calendars 等）失败，BUILD_EXIT=2。
- 根因：Stellarium 桌面版假设 QtPositioning 模块始终存在，`StelLocationMgr.hpp:28` 无条件 `#include <QtPositioning/QGeoPositionInfoSource>`；关 ENABLE_GPS 仅不定义 `-DENABLE_GPS` 宏、不跳过该 include。OHOS Qt 无 Positioning 模块 → 头文件缺失。
- 修复：用 `#ifdef ENABLE_GPS` 包裹该 include（关 GPS 时不需）。源码树仅此一处 QtPositioning include。构建脚本 line130 仅 checkout CMakeLists 两文件，不动 hpp，故直接 Edit 源码可持久、重跑脚本安全。
- 状态：已 Edit 修复，后台重跑脚本增量编译（build6.log），待验证是否还有 ENABLE_GPS=0 路径下其他 Positioning 引用。

## 坑6：StelLocationMgr.cpp 中 GPS 代码漏 #ifdef（build6 仍炸）
- 现象：build6 至 68% 后又报 `StelLocationMgr.hpp:204/241/243 unknown type name 'QGeoPositionInfo'/'QGeoPositionInfoSource'`，且 RemoteControl/Scenery3d 连锁报错（嵌了 StelLocationMgr 成员导致类不完整）。根因：关 ENABLE_GPS 后 hpp 加前向声明，但 .cpp 里构造函数配置定位源(499-508)、析构 delete(513-532)、positionUpdatedFromOS 实现(1002-1038) 均在 `#ifdef ENABLE_GPS` 外，用到完整类型。
- 修复：用 `#ifdef ENABLE_GPS` 包裹这三段（行号法插入，从后往前避免偏移）。ENABLE_GPS 计数 2→5，文件已落盘校验。
- 关键失误：第一次后台命令用 `python3 - <<'PY' ... PY && cd ...` 结构，zsh 报 `parse error near '&&'`（heredoc 结束符后不能行首 &&），导致 python 未执行、编译未启动、build7.log 未生成。✅ 正确写法：heredoc 后换行接 `cd ... && bash ...`（不以 && 开头），或先前台改完再单独后台编译。
- 状态：build7.log 重启编译中（task 待查）。

## 定时巡检 automation 已建
- 用户要求"经常定时检查"避免编译报错卡死。平台 automation 不支持 MINUTELY（最小 HOURLY）。已创建每小时巡检 automation（id automation-1784400504685，validUntil 2026-07-20T03:00），prompt 会查最新 /tmp/stellarium_build*.log、报错自动修复重跑、编完自动 hdc install 到平板 7LZBB26323200303。

## 坑7：链接期 undefined symbol（声明未纳入 #ifdef，build7 卡在 98% 链接）
- 现象：build7 冲到 98% 链接阶段报 `ld.lld: error: undefined symbol: StelLocationMgr::positionUpdatedFromOS(QGeoPositionInfo const&)`（mocs_compilation.cpp 引用），BUILD_EXIT=2。
- 根因：坑6 只把 `positionUpdatedFromOS` 的**实现**（.cpp）用 `#ifdef ENABLE_GPS` 包起，但它在头文件里的**声明**（StelLocationMgr.hpp:210）仍在开关外；Qt 的 moc 仍登记该 slot → 链接时找不到实现。属"声明/实现不对称"导致 moc 引用悬空。
- 修复：把头文件第 210 行 `void positionUpdatedFromOS(const QGeoPositionInfo &info);` 整段挪进 `#ifdef ENABLE_GPS` 块内（连同上方 MAYBE 注释）。已校验：`grep -A14 '#ifdef ENABLE_GPS'` 显示声明已在块内（line 209）。
- 注：成员指针 `positionSource/qGeoPositionInfoSource/nmeaHelper/libGpsHelper` 与 `GPSLookupHelper`（前向声明 `class GPSLookupHelper;` 已存在）在关 GPS 时仅需前向声明（指针用法），不触发链接错误，无需改动。

## 专家团轮巡看门狗（patrol-watchdog）已建并常驻
- 用户原话："你不能再搞个专家团这样轮巡吗？轮巡看看哪地方又停了、哪地方又卡了，然后继续去再找一批专家修复。"
- 落地：写 `phase2/stellarium-ohos-port/build/patrol-watchdog.py` 并后台常驻（nohup，task AHmJRX / boot log /tmp/stellarium_patrol_boot.log，巡逻日志 /tmp/stellarium_patrol.log）。
- 机制（像 6 人专家团轮班）：
  - 每 10 分钟查最新 /tmp/stellarium_build*.log；轮值专家按序：乔渡星(主理)/匡编译/苏星图/包立成/赖库通/严三端。
  - 编译中→报百分比；卡住(BUILD_EXIT=2)→匹配"已知修法知识库"自动修复并重跑；编完("Stellarium OHOS 构建完成")→自动 hvigorw 打包 .hap 并 hdc install 到平板 7LZBB26323200303；未知错误→写升级记录到本记忆文件、停止轮巡、等主理人(杨杰轩)接手。
  - 已知修法（幂等，可扩充）：FIX-1 undefined symbol positionUpdatedFromOS→把声明纳入 #ifdef ENABLE_GPS；FIX-2 缺 QtPositioning 头/类型→补前向声明+include 开关。
- 配套：旧 hourly automation（automation-1784400504685）改为"轮巡守护"——只负责 pgrep 看门狗、死了就 nohup 重启它；**不再自己改源码**，避免两个自动体同时改源码打架。双保险：看门狗 10 分钟密巡 + 守护每小时兜底防看门狗进程意外死亡（如 Mac 睡眠/会话结束）。
- build8（02:56 启动，task Uk3QoF）已带坑7声明修复重跑；看门狗首条轮巡已记录（02:56:08 当值 乔渡星，状态"编译进行中"）。
